flask>=3.0
pillow>=10.0
boto3>=1.34
opencv-python-headless>=4.9
# Only needed if importing iPhone HEIC files directly:
# pillow-heif>=0.16
# Only needed for outline extraction (scripts/extract_outlines.py):
# rembg>=2.0
# onnxruntime>=1.17
